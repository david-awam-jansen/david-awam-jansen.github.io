on:
  push:
    branches: main

jobs:
  print-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Print CR_PAT secret
      run: echo "Does this work!!!"
        run: echo ${{ secrets.CR_PAT }}
